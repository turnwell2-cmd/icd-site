# Render deployment configuration for ICD website
# This file tells Render how to run your PHP site

services:
  - type: web
    name: icd-website
    env: php
    plan: free
    buildCommand: ""
    startCommand: php -S 0.0.0.0:10000 -t .
    autoDeploy: true
    rootDir: .
